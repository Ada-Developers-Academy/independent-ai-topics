| Vocab | Definition | Synonyms | How to Use in a Sentence |
| --------- | --------- | -------- | --------- |
| Volume mount | A mechanism for making a specific directory from the host filesystem accessible inside a container, while keeping the rest of the host filesystem out of scope. | Bind mount, mounted directory | "By mounting only `src/` as a volume, we ensured the agent could write implementation files but had no path to the credentials stored elsewhere on the machine." |
| Permission gating | A configuration in an agentic tool that specifies which categories of action (file reads, file writes, shell execution, network requests) the agent can take freely, must request approval for, or is blocked from entirely. | Tool permissions, allow/deny rules | "Setting shell execution to 'ask' in our permission gating meant the agent paused for approval before running any command, giving us a chance to review it first." |
| Allowlist | An explicit list of operations, paths, or commands that are permitted, with everything else denied by default. | Whitelist | "The allowlist restricted shell commands to `git status`, `git log`, and `npm test`, so the agent could check state and run tests but couldn't make writes through the shell." |